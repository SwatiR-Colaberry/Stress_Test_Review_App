# Root dependency manifest for Stress_Test_Review_App (Python).
# Runtime deps used by backend/app/ (FastAPI + Pydantic API, REQ-015).
fastapi==0.141.1
pydantic==2.13.5
uvicorn==0.53.0

# Runtime dep for backend/app/mcp/server.py (official MCP SDK; requires
# Python 3.10+, see .python-version).
mcp[cli]==2.2.0

# SQL Server access (REQ-013), per docs/enterprise-portal-build-prompt.txt:
# pyodbc over the Microsoft ODBC Driver 18. The driver itself is a system
# install, not a pip package: brew install msodbcsql18 (see
# directives/ST-historical-comment-extraction.md, Step 2).
pyodbc==5.3.0

# Loads the local, git-ignored .env into the environment for operational
# scripts (backend/scripts/). App code reads os.environ only.
python-dotenv==1.2.3

# Test tooling.
pytest==9.1.1
httpx==0.28.1
