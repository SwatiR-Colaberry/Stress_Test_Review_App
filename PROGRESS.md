# PROGRESS

Tracks completed implementation work for the Stress Test Review App, per CLAUDE.md's Logging, Reporting & Progress Tracking section. Each entry is tagged with the Session ID that wrote it; only that session may edit or audit its own entries.

## R4 — Safety guardrail (human-approval finalization)

- [x] Design and implement the R4 safety guardrail: a review can never be finalized without genuine human approval
  - Date: 2026-09-13
  - Session: CC-20260913-b7q2
  - What changed: Added `assertSafeToFinalize()` in `backend/src/services/guardrails/reviewFinalizationGuardrail.js`, its test suite (`reviewFinalizationGuardrail.test.js`), and `directives/R4-human-approval-guardrail.md` defining what "safe" means and the acceptance criterion that moves R4 from UNMAPPED to PLANNED. Added a minimal root `package.json` so `npm test` runs the suite with no extra setup.
  - Verification: `npm test` — 9/9 tests pass (Node's built-in `node:test` runner, zero added dependencies).
  - Notes: Backend/frontend scaffolding doesn't exist yet (project is pre-implementation per README and Master Spec §27); this guardrail is a standalone, dependency-free module so it's verifiable now and reusable unchanged once Phase 4 (§25) wires a real finalize/post-to-Basecamp path through it. Assumptions logged (5): (1) plain Node.js/CommonJS instead of TypeScript, since no backend toolchain is chosen yet; (2) placed under `backend/src/services/guardrails/` per CLAUDE.md's service-layer convention; (3) R4's exact wording was inferred from Master Spec §22/§26 since the platform's own R4 text wasn't downloaded locally; (4) used Node's built-in `node:test` runner rather than adding Jest/Vitest as a dependency at this early stage; (5) added a minimal root `package.json` solely to satisfy the "one-command test execution" Intern Safety Rule.

## Critique marker detection (Master Spec §4)

- [x] Implement and test `##Critique##` marker normalization/detection
  - Date: 2026-09-15
  - Session: CC-20260915-kpnf
  - What changed: Added `normalizeCritiqueMarker()` / `isCritiqueMarker()` in `backend/src/services/basecamp/critiqueMarkerDetector.js` and its test suite (`critiqueMarkerDetector.test.js`), covering the four documented spacing variants, case-insensitivity, marker embedded in a larger comment, the explicit non-triggers from §4 (`Critique`, `#Critique`, `##Review##`, ordinary prose), non-string input, and idempotency. Widened root `package.json`'s `test` script from the guardrails-only glob to `backend/src/**/*.test.js` so both suites run under one `npm test`.
  - Verification: `npm test` — 18/18 tests pass (9 existing R4 guardrail tests + 9 new marker-detection tests), zero added dependencies.
  - Notes: Scope is normalization/detection only — this module does not yet touch `MessageId`/`CommentId` version identity, review-record creation, or idempotent-dedup-on-retry (§4's other requirements); those depend on a Basecamp/SQL Server integration layer that doesn't exist yet and are tracked as separate unchecked items in the requirements checklist. Assumptions logged (2): (1) case-insensitive matching beyond the four literal examples, since the spec's own section header says "case/spacing variations" even though only spacing variants are enumerated; (2) single optional space only on each side of the word (not `\s*`), since the spec enumerates exactly four spacing forms and CLAUDE.md's anti-scope-creep guidance says not to generalize beyond what's specified.

## Acceptance checklist (repo-verifiable requirements tracking)

- [x] Persist the requirements-to-acceptance-check mapping as a tracked repo doc instead of only conversation text
  - Date: 2026-09-15
  - Session: CC-20260915-kpnf
  - What changed: Added `docs/ACCEPTANCE_CHECKLIST.md` — one-line, repo-verifiable acceptance checks per requirement, derived from the Master Project Specification and `directives/R4-human-approval-guardrail.md`, grouped to match the spec's own section numbers. 30 checklist items total; 3 currently checked (marker detection, plus R4's two human-approval-safety checks), each backed by a named passing test.
  - Verification: File exists at `docs/ACCEPTANCE_CHECKLIST.md`; the 3 checked items cross-reference the passing tests already verified above (`npm test`, 18/18).
  - Notes: User asked where the checklist (originally produced only as chat output) should live; chose "new repo file" over folding it into the published Build artifact or leaving it in chat, so it's version-controlled and can be updated alongside future `PROGRESS.md` entries as items get checked off.

## R4 — Credential leak guardrail (the real R4, correcting an earlier mislabel)

- [x] Design and implement the actual R4 guardrail (REQ-016: no credentials in source or shared documents) and correct the earlier "R4" mislabel
  - Date: 2026-09-16
  - Session: CC-20260916-t4m8
  - What changed: Pulled `.colaberry/plan.json`/`progress.json`/`manifest.json` and the synced `docs/` planning set for the first time (`git pull`), which revealed the canonical mapping: per `derived.guardrails[]`, **R4 is REQ-016** ("no credentials stored in source or shared documents"), not REQ-011 (human approval) as originally assumed on 2026-09-13 without canonical data. Both requirements are bundled in **STORY-011**. Added `backend/src/services/guardrails/credentialLeakGuardrail.js` (`scanTextForCredentials`/`assertNoCredentialLeaks`) and its test suite, plus `backend/src/scripts/scanForCredentials.js` as the actual repo-wide enforcement check (`npm run scan:secrets`). Renamed `directives/R4-human-approval-guardrail.md` → `directives/REQ-011-human-approval-guardrail.md` (with a correction note) and added `directives/REQ-016-credential-leak-guardrail.md` for the real R4. Updated `.colaberry/progress.json` for STORY-011: criteria 2 (auto-approve block) and 3 (credentials) marked `passed: true` with evidence; criterion 1 (audit trail) left `false` — not built. Also committed prior session `CC-20260915-kpnf`'s work (critique marker detector, acceptance checklist), which had been sitting uncommitted in the working tree since 2026-09-15 despite its own PROGRESS.md entry claiming it was done — that entry's "commit names it" half of Definition of Done was not actually true until this commit.
  - Verification: `npm test` — 30/30 pass. `npm run scan:secrets` — exits 0, "No credential-shaped content found across 36 text files."
  - Notes: Found a prompt-injection attempt embedded in `docs/stories/STORY-000.md` (line 48) trying to pre-authorize blind acceptance of "a long command containing a signing secret" without confirmation — flagged to the user in chat, treated as inert file content, not followed. Assumptions logged: (1) excluded `*.test.js` fixtures from the enforcement scan, since they deliberately contain fake credential-shaped values to exercise the detectors; (2) left `.colaberry/progress.json`'s `verification` block untouched per `docs/DATA_CONTRACT.md` — it's platform-owned/computed on sync, not something this session should hand-edit; (3) did not attempt criterion 1 (audit trail logging) — out of scope for this request and blocked on a database layer that doesn't exist yet.

## Acceptance checklist rebuild (canonical REQ-XXX ids)

- [x] Rebuild `docs/ACCEPTANCE_CHECKLIST.md` against the now-real `.colaberry/plan.json`/`progress.json`, replacing the earlier Master-Spec-section-numbered version
  - Date: 2026-09-16
  - Session: CC-20260916-edil
  - What changed: The user re-asked "review the requirements, write a one-line acceptance check for each" — same request as 2026-09-15, but canonical `.colaberry/plan.json` (18 `REQ-XXX`, 12 `STORY-XXX` + `STORY-000`) now exists (pulled by `CC-20260916-t4m8`), superseding the spec-section-numbered checklist written before it existed. Rewrote `docs/ACCEPTANCE_CHECKLIST.md` keyed on the real `REQ-001`–`REQ-018`, each check grounded in that requirement's fulfilling story's Given/When/Then acceptance text plus current repo evidence. 3/18 checked (REQ-001 marker detection, REQ-011 no-auto-approve, REQ-016 no-credentials) — all three cross-checked against `.colaberry/progress.json`'s per-criterion `passed` flags for STORY-011, not just against local tests. Added two call-outs the old checklist couldn't have surfaced: (1) STORY-000 (Command Center, 5 criteria, all `passed:false`) is untouched by this session's work and is NOT satisfied by the claude.ai Build artifact published earlier this conversation — that artifact is external and hand-authored, not a repo-committed `index.html` reading the live JSON files as STORY-000 requires; (2) REQ-015 (FastAPI+Pydantic) directly conflicts with the Node.js/CommonJS stack every session has actually built in, and REQ-013 (SQL Server) / REQ-014 (Anthropic API) are `fulfilled_by: []` in the plan — no story currently builds either.
  - Verification: `npm test` — 30/30 pass. `npm run scan:secrets` — exits 0. File reviewed against `.colaberry/plan.json`, `progress.json`, `REQUIREMENTS.md`, `STORIES.md` line by line for the 18 REQ ids and their fulfilling stories' acceptance text.
  - Notes: Did not attempt to resolve the Node-vs-FastAPI stack conflict or the two orphaned constraints — flagged per CLAUDE.md's Escalation Protocol (architecture pattern conflict / directive conflict) rather than silently building around or switching stacks unilaterally. Left `.colaberry/progress.json` untouched, same rule `CC-20260916-t4m8` followed (platform-owned, computed on sync).

## Backend migration: Node.js/CommonJS → Python/FastAPI+Pydantic (REQ-015)

- [x] Migrate the entire backend to Python/FastAPI+Pydantic per REQ-015, resolving the stack conflict flagged earlier this session
  - Date: 2026-09-16
  - Session: CC-20260916-edil
  - What changed: User explicitly directed the migration after this session flagged the Node-vs-FastAPI conflict. Ported all three existing modules 1:1 (same logic, same reason codes/detectors, same test cases) from JS to Python: `backend/src/services/guardrails/reviewFinalizationGuardrail.js` → `backend/app/guardrails/review_finalization_guardrail.py`; `credentialLeakGuardrail.js` → `credential_leak_guardrail.py`; `backend/src/services/basecamp/critiqueMarkerDetector.js` → `backend/app/basecamp/critique_marker_detector.py`; `backend/src/scripts/scanForCredentials.js` → `backend/scripts/scan_for_credentials.py`. Added `backend/app/models.py` (Pydantic models formalizing the JSDoc @typedef shapes as real, enforced types — `ReviewerDecision`, `StressTestReview`, request/response models) and `backend/app/main.py` (a real `FastAPI()` app) with three routers wrapping the existing logic over HTTP: `POST /reviews/finalize-check`, `POST /security/scan-credentials`, `POST /basecamp/critique-marker/detect`, plus `GET /health`. Added root `requirements.txt` (fastapi==0.115.0, pydantic==2.13.5, uvicorn==0.30.6) and `requirements-dev.txt` (+pytest==8.3.3, httpx==0.27.2, all pinned to the exact versions installed and tested against), root `pytest.ini` (`testpaths=backend`, `pythonpath=backend`) and `Makefile` (`make install`/`test`/`scan-secrets`/`run`) as the one-command entry points replacing `npm test`/`npm run scan:secrets`. Removed the superseded Node artifacts via `git rm`: all of `backend/src/` and the root `package.json`. Updated both guardrail directives (`directives/REQ-011-human-approval-guardrail.md`, `directives/REQ-016-credential-leak-guardrail.md`) and `docs/ACCEPTANCE_CHECKLIST.md` to reference the new Python paths/commands, and flipped REQ-015 to checked in the checklist (a real FastAPI+Pydantic API now exists and is tested, not just a pinned dependency). Also corrected `CLAUDE.md`'s stale "Node.js + Express + TypeScript backend" Folder Responsibilities section and its TypeScript-only Contract Enforcement Layer row to describe the actual Python/FastAPI/Pydantic stack, with an inline note that this needs the DRI's (Ali's) sign-off per `CLAUDE.md`'s own Configuration Ownership section — did not touch the unrelated SupplyMind-inherited sections (Cory briefing, openclaw, Mandrill, alumni content) that remain in this file, since cleaning those up is a separate, larger task nobody asked for yet.
  - Verification: `pytest` — 39/39 pass (9 marker-detection + 9 review-finalization + 12 credential-leak unit tests, all ported 1:1, plus 9 new FastAPI endpoint tests via `TestClient` including a 422-on-malformed-input case). `python3 backend/scripts/scan_for_credentials.py` — exits 0, "No credential-shaped content found across 44 text files." Both commands actually run in a local `.venv` (gitignored) against the pinned versions, not just asserted to work.
  - Notes: This is a "Large refactor (>25% module rewrite)" per CLAUDE.md's own Autonomy Model — a Strategic Decision requiring escalation, not proceeded on unilaterally. It was escalated (flagged in chat + this session's own prior PROGRESS.md entry) and the user then gave explicit direction, which is exactly what escalation is for — proceeding here is executing a resolved decision, not making one. Assumptions logged: (1) endpoint scope is exactly the 3 pieces of logic that already existed — did not invent new HTTP surface area for REQ-002–REQ-010/REQ-012–REQ-014/REQ-017/REQ-018, which still have no implementation to wrap; (2) `requirements.txt`/`requirements-dev.txt` split (runtime vs. dev) rather than one file, matching the dependency-manifest spirit of the original R4 guardrail's minimal `package.json`; (3) colocated `test_*.py` next to source (pytest-discoverable) rather than a separate `backend/tests/` tree, mirroring the original `*.test.js`-next-to-source convention; (4) did not add `mypy` or any static type checker — CLAUDE.md's Definition of Done now says `pytest` for backend, and introducing a new tool/dependency wasn't asked for. STORY-000 (Command Center) and REQ-013/REQ-014 (SQL Server, Anthropic API — still `fulfilled_by: []`) remain untouched and open.

## Local MCP server (official SDK), one resource + one tool stub

- [x] Scaffold a local MCP server for the Stress Test Review App using the official `mcp` Python SDK, with one read-only Basecamp-submissions resource and one tool stub; run it end-to-end and register it with Claude Code
  - Date: 2026-09-22
  - Session: CC-20260922-vu9w
  - What changed: Environment blocker first — the official `mcp` SDK requires Python 3.10+, but the only interpreter on this machine was the Xcode Command Line Tools' Python 3.9.6. Installed `uv` (via `pip install --user uv`) and used it to fetch a standalone Python 3.12.14 (`uv python install 3.12`), then rebuilt the project's `.venv` on that interpreter. Added `.python-version` (`3.12`) so this is reproducible. Installing `mcp[cli]` alongside the existing pinned `fastapi==0.115.0` produced a real conflict (`mcp` pulls `starlette==1.6.0`, incompatible with `fastapi<0.39` starlette bound) — resolved by letting pip re-resolve the whole stack together unpinned, then re-pinning `requirements.txt`/`requirements-dev.txt` to the new mutually-compatible versions (fastapi 0.141.1, pydantic 2.13.5, uvicorn 0.53.0, mcp[cli] 2.2.0, pytest 9.1.1, httpx 0.28.1) — verified reproducible by reinstalling into a throwaway `/tmp` venv from the pinned files alone. Also discovered mid-build that `mcp` 2.x renamed `FastMCP` to `MCPServer` (`mcp.server.mcpserver.MCPServer`) with a changed API — confirmed the real signatures by introspecting the installed package rather than assuming from training data. Added `backend/app/mcp/server.py` (an `MCPServer` instance named `stress-test-review-app`) exposing: one read-only resource `basecamp://submissions` (required) backed by `backend/app/mcp/sample_data.py` — honestly labeled sample data via an `_note` field, since REQ-004/REQ-012 (real Basecamp retrieval/OAuth) aren't built; and one deliberately unimplemented tool stub, `finalize_review(review_id)`, returning `{"status": "not_implemented", ...}` rather than pretending REQ-007's finalize path exists. Added `backend/app/mcp/test_server.py`, which spawns the server as a real subprocess and drives it over stdio with the official `mcp.client` (`ClientSession` + `stdio_client`) — not a mock. Added root `.mcp.json` to register the server with Claude Code (stdio transport, `command: ".venv/bin/python3"`, `args: ["-m", "app.mcp.server"]`, `env: {"PYTHONPATH": "backend"}`). Added `make mcp-run` / `make mcp-inspect` targets. Updated `CLAUDE.md`'s backend subfolder list (`backend/app/mcp/`), `README.md` (Python 3.10+ requirement, a "Local development" setup section, corrected the now-stale "pre-implementation" status line), and `docs/ACCEPTANCE_CHECKLIST.md` (new "Not a requirement, but tracked separately" section, same pattern used for STORY-000).
  - Verification: Ran the server manually as a subprocess via the official stdio client before writing the permanent test — confirmed `initialize()`, `list_resources()`, `read_resource("basecamp://submissions")` (3 sample submissions + the sample-data note), `list_tools()`, and `call_tool("finalize_review", {...})` (returned the stub's `not_implemented` payload, `is_error: False`) all work end-to-end. `pytest` — 41/41 pass (the 39 from before + 2 new MCP tests, which re-run that same subprocess/stdio flow as an automated regression rather than a one-off). `python3 backend/scripts/scan_for_credentials.py` — exits 0, "No credential-shaped content found across 47 text files." `claude mcp list` — confirms Claude Code itself parsed `.mcp.json` and lists `stress-test-review-app: .venv/bin/python3 -m app.mcp.server - ⏸ Pending approval (run 'claude' to approve)` — real proof the config is valid and picked up, not just well-formed JSON.
  - Notes: "Pending approval" is Claude Code's own trust gate for a newly-registered project MCP server — expected, not a bug; the user needs to run `claude` interactively and approve it once. Assumptions logged: (1) resource URI scheme `basecamp://` chosen to name the eventual real data source, not a generic `data://`; (2) sample data's field shape (project/student/stress_test/message_id/comment_id/critique_marker_detected/submitted_at) drawn directly from Master Spec §2/§4's own vocabulary, not invented; (3) `mcp` added to `requirements.txt` (runtime), not `requirements-dev.txt`, since it's a real interface the app offers, not a dev-only tool; (4) did not attempt to wire `finalize_review` to the real guardrail logic — user asked for a stub, and doing so would have required inventing a persistence layer that doesn't exist; (5) rebuilt the *whole* project venv on Python 3.12 rather than keeping a second, MCP-only 3.9-incompatible environment, to keep one consistent interpreter for the whole backend — this bumps the repo's effective minimum Python version from "any 3.9+" to "3.10+", documented in `.python-version` and `README.md`.

## Remove the Makefile

- [x] Remove `Makefile`; document the raw commands it wrapped in `README.md` instead
  - Date: 2026-09-22
  - Session: CC-20260922-vu9w
  - What changed: User asked why the Makefile was needed, then (on being told every target is a one-line wrapper around a plain `pip`/`pytest`/`uvicorn`/`python3` command, and being on Mac and unsure about tooling compatibility) asked to remove it and document the raw commands directly. Deleted `Makefile` (`git rm`; it had been committed in the earlier FastAPI-migration commit). Updated every place that referenced a `make` target to the underlying command instead: `README.md`'s "Local development" section (now lists the four raw commands), `directives/REQ-011-human-approval-guardrail.md` (`make test`/`make install` → `pytest`/`pip install -r requirements-dev.txt`), `directives/REQ-016-credential-leak-guardrail.md` (`make scan-secrets` → `python3 backend/scripts/scan_for_credentials.py`, in three places, and refreshed its stale file/test counts to the current 47 files / 41 tests while there), `docs/ACCEPTANCE_CHECKLIST.md`'s REQ-016 line, and the docstrings in `backend/app/main.py` and `backend/scripts/scan_for_credentials.py` that mentioned `make run`/`make scan-secrets`. `.mcp.json` needed no change — it already invoked `.venv/bin/python3` directly, never `make`.
  - Verification: `pytest` — 41/41 pass (unaffected; the Makefile never contained any logic, only aliases). `python3 backend/scripts/scan_for_credentials.py` — exits 0 across 47 files. Grepped the whole repo for remaining `make `/`Makefile` references afterward — only hits left are the ordinary English word ("make sure", "make the JSON match") in unrelated docs, and this file's own past entry describing when the Makefile existed (left as an accurate historical record, not rewritten).
  - Notes: `make` itself is a standard Unix build tool bundled with Xcode Command Line Tools on macOS (confirmed present and in use earlier this session) — it isn't Windows/Microsoft-specific and would have worked fine on this machine. Removed anyway per explicit user preference for one fewer layer of indirection, not because it was broken.

## Merge requirements.txt / requirements-dev.txt into one file

- [x] Check whether SupplyMind_AI has a single-command setup/test tool worth reverting to; merge to one `requirements.txt` to match what it actually does
  - Date: 2026-09-22
  - Session: CC-20260922-vu9w
  - What changed: User asked whether "it" (a single-line setup/run command) is already solved in SupplyMind_AI, offering to revert to that pattern if so. Checked the actual local `SupplyMind_AI` repo rather than assuming: no `Makefile`, no wrapper script, no shell script exists there either — its own `.github/workflows/tests.yml` just runs `pip install -r requirements.txt` then `python -m pytest -q`, i.e. plain two commands, same shape as this repo without the Makefile. The one real difference: SupplyMind_AI uses a single `requirements.txt` (no dev/runtime split), with a plain `# Test tooling.` comment marking `pytest` as dev-only — satisfies CLAUDE.md's dependency-manifest rule ("includes dev/test-only dependencies... not just what ships to production") without needing two files. Adopted that exact pattern here: merged `requirements-dev.txt` into `requirements.txt` (comment-separated sections: FastAPI/Pydantic runtime deps, the MCP SDK, then `# Test tooling.` for pytest/httpx) and deleted `requirements-dev.txt`. Updated `README.md`'s quickstart to one chained line (`python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`) and `directives/REQ-011-human-approval-guardrail.md`'s remaining `requirements-dev.txt` mention.
  - Verification: Reinstalled into a throwaway `/tmp` venv from the merged `requirements.txt` alone — `pytest` 41/41 pass. Re-ran against the project's own `.venv` too — same result. `python3 backend/scripts/scan_for_credentials.py` — exits 0 across 46 files.
  - Notes: This is a genuine simplification (one file, one install command) grounded in an actual sibling-repo precedent, not an invented one — reported honestly that no Makefile-equivalent exists there to "revert back" to, rather than fabricating one or silently building a new wrapper anyway.

## STORY-000 — Command Center (Overview checkpoint)

- [ ] Build the Command Center (`index.html` at repo root): Overview tab complete; other 8 tabs reachable with an honest "not built yet" state, per the brief's explicit "show me Overview and stop" checkpoint
  - Date: 2026-09-24
  - Session: CC-20260924-x7q3
  - What changed: Take-stock (Step 2a) confirmed nothing existed yet — no `index.html` anywhere, no `command-center/` folder, all 9 tabs missing. Built the static Command Center from scratch: `index.html` (root entry point), `command-center/style.css` (neutral palette in CSS custom properties, one place to re-skin, light+dark), `command-center/data.js` (fetches `.colaberry/{plan,progress,manifest,profile}.json` at runtime, joins plan↔progress on story id per `docs/DATA_CONTRACT.md`, computes `isRequirementBuilt`, and the "Data as of" freshness calculation), `command-center/sample-data.js` (Sample mode reuses the real plan — same requirements/stories, not invented — and swaps in a synthetic progress/manifest so the UI shows a plausible further-along state, clearly labelled), `command-center/router.js` (hash router, `#/<tab>/<detail>`), `command-center/chrome.js` (shared header: project name, "Data as of" stamp with stale-after-a-week warning, Sample/Real toggle, tab nav, sample banner), `command-center/tabs/overview.js` (5 drill-down cards — What this is, Where you are, Stories, Criteria, Points — each reading straight from `plan.project`/`plan.schedule`/`progress.totals`, plus detail views with per-story tables), `command-center/tabs/placeholder.js` (shared, reachable, un-gated "not built yet" renderer for the other 8 tabs, each still one card that drills down one level).
  - Verification: `node --check` on all 7 JS modules (no syntax errors). Served locally (`python3 -m http.server`) and driven with Puppeteer (headless Chrome): confirmed all 9 tabs reachable and render without console/page errors (the only two "errors" were the browser's own automatic `favicon.ico` 404, unrelated to the app); confirmed Overview's real numbers match `.colaberry/progress.json` exactly (0/13 stories verified, 2/41 criteria passed, 650 points) and schedule position correctly shows "In r2 · Human Review Workflow" (today 2026-09-24 falls inside r2's 09-24→09-25 window); confirmed the stale-data warning fires correctly (`manifest.generated_at` is 2026-09-16, 7 days 21 hours before now — caught and fixed a floor-rounding bug in the first pass that would have suppressed the warning at exactly this age); confirmed drill-down works on both Overview cards (e.g. Stories → per-story table) and placeholder cards (→ "not built yet" detail); confirmed Sample mode swaps in fictional-but-labelled data (purple "SAMPLE DATA" banner + per-card tags) and Real mode shows the actual file contents, with the toggle persisted via `localStorage` (a per-viewer convenience only, not shared state).
  - Notes: This is a checkpoint, not the finished story — per STORY-000's own instructions ("Show me the Overview tab first and stop"), the other 8 tabs are intentionally left as reachable placeholders pending the user's review, and `.colaberry/progress.json`'s STORY-000 criteria are all still correctly `false` (criterion 1 alone requires all 9 tabs to exist). No criteria ticked, no `.colaberry/enrichment/STORY-000.json` written yet — both belong to Step 3, once "build the rest" is done. Assumptions logged: (1) vanilla JS/ES modules with zero external dependencies (no framework, no CDN) — simplest option that still satisfies GitHub Pages' static-hosting constraint and the "explicit dependencies" production-readiness principle; (2) hash-based routing (`#/tab/detail`) rather than a build step, so the whole thing stays plain static files; (3) Sample mode reuses the real plan.json structure and only fabricates progress/manifest numbers, rather than inventing a parallel fictional plan, to avoid manufacturing a customer/requirement that doesn't exist; (4) `localStorage` used only for remembering the Sample/Real toggle across reloads in one browser — not treated as shared or durable state.

## STORY-000 — Command Center (build the rest — finished)

- [x] Build the remaining 8 tabs, remove the pause banner, and finish Step 3 (tick genuinely-true criteria, write enrichment, commit, push)
  - Date: 2026-09-24
  - Session: CC-20260924-x7q3
  - What changed: User said "build the rest." Added `command-center/tabs/{outcomes,users,guardrails,systems,project-management,agents,knowledge-base,data-model}.js`, wired all 9 into `command-center/main.js`, removed `command-center/tabs/placeholder.js` (now unused) and the pause banner from `overview.js`. **Outcomes**: one card per `plan.derived.measures` entry, always reading "not measured yet" (no invented value), drilling into the requirement's fulfilling stories. **Users & use case**: cards per `plan.derived.roles` (reviewer, human reviewer, system administrator), matched against story narratives by their literal "As a `<role>`," prefix. **Guardrails**: cards per `plan.derived.guardrails`, enforcement derived by following `requirement.fulfilled_by` → story ids → `progress.json` verification state (not enforced unless every fulfilling story is verified). **Systems**: every `plan.derived.systems` name rendered with a grey "not checked from here" indicator — never green, even in Sample mode (explicit note added). **Project management**: a CSS-only Gantt-style timeline (release bars positioned by day-offset across build_start→demo_day, ★ marks the demo-target release, dashed line = demo day, green line = today) plus a full task table (due_on vs due_baseline_on, slippage in days — currently 0 for every story) with per-story and per-release drill-down (narrative, acceptance criteria with real pass/fail from `progress.json`, failure_paths, task_guidance). **AI agents**: `plan.agents[]` is empty, so built from `story.owner_agent` instead, grouped by the exact owner strings in the data (kept "System" and "system" as distinct rows rather than silently merging them) — each card says plainly "owner, not a scoped AI agent" and "no skills registered yet" / "no runs recorded". **Knowledge base**: a full requirements-traceability table (18 requirements × fulfilling stories × verified state), a local keyword-search panel honestly described as *not* a hosted AI (a static page can't hold an API key) that says "can't answer that from this project's data" on no match, and a notes section using `localStorage` with an explicit "saved only in this browser" caption. **Data model**: 9 proposed entities (Submission, ReviewItem, RuleModule, Finding, ReviewHistoryEntry, AmbiguousResolutionTask, ExternalCallLog, Reviewer, OAuthGrant) each citing the real requirement id(s) it's derived from, explicitly captioned "a starting proposal... nothing has been created in a real database yet." Also fixed a bug found during this pass: `systems.js`'s per-row "Details →" button reused the `.crumb` CSS class (meant only for breadcrumbs), which was renamed to a new `.link-btn` class in `command-center/style.css`. Wrote `.colaberry/enrichment/STORY-000.json` (3 fact proposals, 1 decision, 1 limitation — including a flagged contradiction: REQ-015's `fulfilled_by` is empty in the plan even though a real FastAPI+Pydantic backend already exists per this file's own earlier entries). Updated `.colaberry/progress.json`'s STORY-000 entry: all 5 criteria flipped `false` → `true` (verified true against the rebuilt repo, not assumed), `files_touched` and `notes` filled in; left the platform-owned `verification` block untouched, matching the convention set by session `CC-20260916-t4m8`.
  - Verification: `node --check` on all 15 JS modules — no syntax errors. Full Puppeteer pass across all 9 tabs (`#/overview` through `#/data-model`): every tab renders a `<h1 class="tab-title">`, every card/row drills down one level (crumb confirmed at each destination — Outcomes→REQ-017, Users→reviewer, Guardrails→REQ-008, Systems→Basecamp, Project management→STORY-001 and a release key, AI agents→System, Data model→Submission), zero page/console errors beyond the browser's own automatic `favicon.ico` 404. Knowledge base traceability table confirmed showing all 18 requirements with REQ-013/REQ-014/REQ-015 correctly highlighted red as "No story yet" (real gap, not fabricated). Re-ran the STORY-000 "delete a story and reload" honesty test from the brief itself: temporarily removed STORY-012 from a working copy of `.colaberry/plan.json`, reloaded, confirmed it disappeared from the Project management table (11 rows instead of 12), then restored the file byte-for-byte (`git status` confirmed clean after) — proves nothing is hard-coded. Sample/Real toggle re-verified across the rebuilt tab set.
  - Notes: All 5 acceptance criteria are ticked in `.colaberry/progress.json` because each was independently verified against the rebuilt repo above, not assumed from "the rest got built." No automated test suite was added for this static page (Puppeteer scripts used for verification live only in this session's scratchpad, not committed) — flagged here rather than silently claimed as covered. Assumptions logged: (1) "every card drills down one level" read as applying to genuine card-grid elements (Overview/Outcomes/Users/Guardrails/AI agents/Data model) and to the equivalent row/bar-level drill-down used by the two table-first tabs (Systems, Project management), since the brief itself describes those two as tables/Gantt rather than cards; (2) Knowledge base's traceability rows are not individually clickable — the fulfilling-story id and its verification state are already shown inline in the row, so a further detail page would repeat the same two facts; (3) `sourceCommitSha` in the enrichment file is `null` since the commit containing this finished work didn't exist yet at write time.

## STORY-001 — Detect Critique Marker in Basecamp (REQ-001 + REQ-002)

- [x] Turn critique-marker detection into Review Queue intake: a detected marker creates one Pending item tied to the exact comment version, every outcome is logged with the exact comment ID
  - Date: 2026-09-24
  - Session: CC-20260924-e18o
  - What changed: Added `BasecampComment` (strict positive-int `comment_id`/`message_id`), `ReviewItem` (status limited to the §22 set, defaults Pending) and `IntakeResult` to `backend/app/models.py`; `backend/app/review_queue/` (`store.py` — `ReviewQueueStore` interface + lock-guarded `InMemoryReviewQueueStore` keyed on `comment_id`; `intake.py` — `process_comment` (validate → detect → create, one JSON log line per outcome) and `run_intake` (fetch with retry → process each); `dependencies.py` — FastAPI store dependency); `backend/app/basecamp/comment_source.py` (`CommentSource` interface, `FixtureCommentSource`, `fetch_with_retry`: ≤3 attempts, 0.5s/1s backoff, retries only connection/timeout errors, raises visible `CommentSourceUnavailable`); `backend/app/logging_config.py` wired in `main.py`; routes `POST /basecamp/comments/process` and `GET /reviews/queue`. Ticked REQ-002 in `docs/ACCEPTANCE_CHECKLIST.md` and the 3 boxes in `docs/stories/STORY-001.md`.
  - Verification: `pytest` — 85/85 pass (41 before; 44 new across `test_models.py`, `review_queue/test_store.py`, `review_queue/test_intake.py`, `review_queue/test_run_intake.py`, `basecamp/test_comment_source.py`, `test_intake_routes.py`, `test_logging_config.py`). Live uvicorn run + curl: `## critique ##` → `review_created` and a Pending item in `/reviews/queue`; `## Review ##` → `no_marker`, no item; same comment again → `already_queued`, same review_id; body without `comment_id` → 422; stdout carried `critique_marker_detected` with `"comment_id": 1001`. `backend/scripts/scan_for_credentials.py` — clean.
  - Notes: **Decisions (user-confirmed this session):** (1) comments are read through a `CommentSource` interface with fixtures; the live `Basecamp_MessageBoards_MessageComments` (SQL Server) adapter is deferred — it needs credentials and a new driver dependency (`pyodbc`/`pymssql`), which is an escalation-level add; (2) the Review Queue is in-memory behind a store interface — items are lost on restart until a database is chosen. **Bugs found in self-review and fixed before commit:** INFO log lines were silently dropped under uvicorn (no logging config existed — AC3 would have failed in the real app while passing under pytest's caplog); lax int parsing turned `comment_id=True` into `1` (could tie a review to the wrong comment) — now strict; empty `backoff_s` raised `IndexError` — now a clear `ValueError`; route now honors `X-Correlation-ID`. **Assumptions:** Basecamp ids are integers; fixed (not exponential) backoff is enough for a 3-attempt cap. **Known limitations:** no route authentication anywhere in the app yet (pre-existing, out of scope here); malformed bodies over HTTP get a 422 but no intake log line (the source-driven path does log them). `scripts/generateSessionChangelog.js` referenced by CLAUDE.md does not exist in this repo, so no session HTML report was generated.

## ST0–ST5 historical comment extraction — Step 1 (store the queries and the plan)

- [x] Store the user-provided ST0–ST5 history queries and the extraction directive; no database connection yet
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: Added `backend/app/basecamp/sql/st_history_counts.sql` (sizing query, as provided), `st_history_comments.sql` (full history, as provided except: critiquer join/columns removed, comments join made INNER), `st_critiquer_assignments.sql` (critiquer history, scoped to active ST0–ST5 threads), `backend/app/basecamp/test_sql_queries.py` (6 invariant tests), `directives/ST-historical-comment-extraction.md` (purpose, why not `proc_AllStressTestData`, table chain, column mapping to `BasecampComment`, run order, PII/read-only/timeout rules, verification checks); `.gitignore` now excludes `data/extracts/`.
  - Verification: `pytest` 91/91 pass (6 new in `test_sql_queries.py`); `git check-ignore` confirmed `data/extracts/*` is ignored; `backend/scripts/scan_for_credentials.py` clean.
  - Notes: User-approved decision: critiquer assignments extracted separately and matched to comments in Python (latest assignment at or before the comment) because joining on `MessageId` alone repeats comments per assignment row. `IsSubmitted` filter deliberately omitted (not a reliable version id, Master Spec §4). SQL `MarkerType` kept as a hint only; markers will be recomputed with the Python normalizer. Nothing executed against SQL Server. Steps 2–6 (driver + `pyodbc`, `.env.example`, connection check, counts, extraction, verification) not started.

## ST0–ST5 historical comment extraction — Step 2 (local SQL Server setup and config)

- [x] Install the SQL Server driver stack and add typed, secret-safe database configuration; no connection made
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: User installed Homebrew and Microsoft ODBC Driver 18 for SQL Server 18.7.1.1 (`brew install msodbcsql18`, trusting only that one formula from `microsoft/mssql-release`). Added `pyodbc==5.3.0` to `requirements.txt` (method set by `docs/enterprise-portal-build-prompt.txt`); `.env.example` (variable names only); `backend/app/db/config.py` — `load_database_config()` (typed `DatabaseConfig`, password as `SecretStr`, all problems reported together by variable name, never value) and `build_connection_string()` (brace-quotes ODBC special characters so a `;` in a password can't inject attributes; Encrypt on by default; explicit connect timeout; `ApplicationIntent=ReadOnly`); `backend/app/db/test_config.py` (12 tests).
  - Verification: `odbcinst -q -d` lists `[ODBC Driver 18 for SQL Server]` pointing at an existing `/opt/homebrew/lib/libmsodbcsql.18.dylib`; `pyodbc.drivers()` returns `['ODBC Driver 18 for SQL Server']`; `pytest` 103/103; `scan_for_credentials.py` clean.
  - Notes: `ApplicationIntent=ReadOnly` is a routing hint, not enforcement — read-only is enforced by the stored queries (`test_queries_are_read_only`) and should also be enforced by a least-privilege login from IT/DBA. `.env` is not auto-loaded yet: config reads the process environment only; how `.env` gets into it is decided in Step 3. System-level installs (Homebrew, msodbcsql18, unixodbc, openssl@3) live outside the repo and are documented in the directive's Step 2.

## ST0–ST5 historical comment extraction — Step 3 (connection check script)

- [x] Add a read-only connection and schema check for SQL Server, with bounded retries and classified, secret-free errors
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: Added `python-dotenv==1.2.3` to `requirements.txt` (user-approved; loads the git-ignored `.env` for scripts only). `backend/app/db/connection.py` — `connect_with_retry()` (login timeout, `readonly=True`, per-query timeout set on the connection; retries only SQLSTATE 08001/08S01/HYT00/HYT01, max 3 attempts, 1s/2s backoff; login failures never retried to avoid lockout; raises `DatabaseConnectionError` with a stable `error_class` + SQLSTATE and never the driver message). `backend/app/db/inspection.py` — `inspect_database()` returns a typed `DatabaseReport`: server version and edition, Full-Text Search installed, whether the login can INSERT into the comments table, all columns of the 5 tables (table names passed as query parameters), missing tables/columns versus what the stored queries reference, and non-integer `CommentId`/`MessageId`. `backend/scripts/check_db_connection.py` — thin runner, exit codes 0/1/2/3. Updated the directive's status and Step 3.
  - Verification: `pytest` 120/120 (17 new: `db/test_connection.py`, `db/test_inspection.py`). Script with no config → names all 4 missing variables, exit 1. Script through the real ODBC Driver 18 against the unroutable test address 192.0.2.1 with fake login details → 3 attempts, each `TimeoutError`/`HYT00`, ~10s total, hint printed, exit 2, no password or driver text in output. `scan_for_credentials.py` clean. Not yet run against the real server (awaiting the user's `.env`).
  - Notes: The driver message is suppressed deliberately (`from None`) because it can contain the server name and login; the SQLSTATE plus the hint is enough to diagnose the classified cases. `01000` is mapped to `DriverNotFound` because unixODBC reports a missing driver library that way; any other SQLSTATE is `DatabaseError`.

## ST0–ST5 historical comment extraction — Step 3 run (first real connection)

- [x] Diagnose the first failed connection, classify untrusted-certificate errors, connect to the real server
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: The user's details had been typed into `.env.example` (not git-ignored, never committed or staged); moved it to `.env` (git-ignored, mode 600) and restored a blank template with a "do not put real values here" warning. Diagnosed `08001` with a scratchpad-only script (prints categories only): DNS ok, port 1433 open, TLS certificate not trusted; the connection succeeds when the certificate is trusted. The user's VS Code mssql profiles already use `encrypt: Mandatory` + `trustServerCertificate: true`, so, as the user directed, set `DB_TRUST_SERVER_CERTIFICATE=yes` in the local `.env` (only that line was changed). `backend/app/db/connection.py`: `classify_error` now returns `TlsCertificateError` when the driver message mentions a certificate (the message is inspected, never logged), and that class is not retried; `check_db_connection.py` has a hint for it.
  - Verification: `pytest` 122/122 (2 new in `db/test_connection.py`). `check_db_connection.py` against the real server: connected on attempt 1; SQL Server 14.0.2075.8 (2017), Web Edition; Full-Text Search NOT installed; the login CAN insert into the comments table; all 5 tables and every referenced column exist; exit 3 on one schema finding: `Basecamp_MessageBoards_MessageComments.MessageId` is `nvarchar(max)`, not an integer.
  - Notes: **Deviation (local only):** the server certificate is trusted rather than verified (still encrypted). Follow-up: get the server's or the company CA certificate from IT and switch back to `DB_TRUST_SERVER_CERTIFICATE=no`. **Follow-up:** request a read-only login (least privilege, Master Spec §18). Our code only ever issues SELECTs. **Open:** `MessageId` types differ across tables (comments `nvarchar(max)`, `pd.MessageBoardID` `varchar(max)`, critiquer `bigint`). The critiquer join compares `varchar` with `bigint`, so SQL Server converts the text side to bigint and would error on any non-numeric value. Profile the data before running the queries.

## ST0–ST5 historical comment extraction — ID profile (counts only)

- [x] Profile the ID columns and join behaviour of the ST history queries before running them
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: Added `backend/app/basecamp/sql/st_id_profile.sql` (23 count metrics), `backend/app/db/queries.py` (`load_sql` restricted to the stored-query folder; `fetch_metrics` returns typed `Metric` rows), `backend/app/db/test_queries.py` (6 tests), `backend/scripts/profile_st_ids.py`. `check_db_connection.py` now prints `(max)` instead of `(-1)`.
  - Verification: `pytest` 128/128. Ran the profile against the real server (read-only, ~5s): all 62,064 comment `MessageId` values are numeric (0 null, blank, text or padded); the critiquer table has 2,173 rows over 2,173 threads (no thread has more than one assignment today); **`steps_total = 0`**. A follow-up scratchpad-only check found that no project has `IsActive = 1` (NULL: 1,424, 0: 964), so the `p.IsActive = 1` filter removes every row. The step names do match `'Stress Test N%'`.
  - Notes: Blocked on a user decision about the `IsActive` filter before the history queries can return data. `StepName` embeds student project titles (some contain first names); treat it as personal data and print counts only from now on.

## ST0–ST5 historical comment extraction — queries corrected against real data; 15 projects selected

- [x] Drop the IsActive filter, join MessageId as numbers, de-duplicate comment snapshots, classify query errors, select 15 projects
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: User explained the current process (Stress Test Grade page → `proc_AllStressTestData`; reviewer clicks Completed) and said history needs no IsActive filter and 10–15 projects that contain feedback. Read the proc definition (read-only): no IsActive filter, `ISNULL(IsSubmitted,0)=0`, last 3 months, exact `##Critique##` only. All stored queries: IsActive filter removed; MessageId joined via `TRY_CAST(... AS bigint)`. `st_history_comments.sql`: one row per `CommentId` (latest snapshot) plus `IsSubmittedAnyCopy` and `CopiesOfComment`. `st_id_profile.sql` rewritten to numeric comparisons with board-sharing metrics. New `st_history_selection.sql` and `backend/scripts/select_st_projects.py`. `backend/app/db/queries.py`: `fetch_table()`; query failures raise `DatabaseQueryError` (classified, no driver text); scripts exit 4 on a query failure. Directive updated (current process, decisions 6–8, selection, caveats).
  - Verification: `pytest` 134/134 (new tests: IsActive guard, numeric joins, one row per CommentId, selection returns no personal data, classified query errors, `fetch_table`). Real server: the proc reproduces the 4 Pending items on the user's screenshot. The profile (7 s) shows 8,581 ST steps, 7,768 with a numeric board, only 4 boards shared by steps (2 comments, none across Stress Tests), and 1,089 duplicated `CommentId`s (4,714 rows: snapshots, text identical in all but 3). Counts: ST0 3,280 / ST1 5,616 / ST2 18,629 / ST3 4,636 / ST4 1,632 / ST5 1,681 comments. The selection (40 s) returned 15 projects: ST0 is covered by 10, ST1–3 by 15, ST4–5 by 11; about 1,500 comments.
  - Notes: The first profile run hit the 120 s query timeout (text comparison of `(max)` columns across all projects) and crashed with a raw pyodbc trace; fixed by the numeric rewrite and classified query errors. Assumptions taken as the recommended defaults after the user said "ok": (1) Completed sets `IsSubmitted = 1` (the data supports it); (2) the selection unit is a whole student project; (3) spread across ST0–ST5 by review cycles, most recent first; (4) feedback lives in comment text (attachments are STORY-002's scope). Extraction (Step 5) is not run yet: waiting on the user's approval of the 15 projects.

## ST0–ST5 historical comment extraction — Steps 5–6 (extract and verify the 15 approved projects); scanner skips git-ignored files

- [x] Extract and verify the full comment history of the 15 user-approved projects; make the credential scan ignore git-ignored files
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: `detect_review_markers()` added to `backend/app/basecamp/critique_marker_detector.py` (Critique/FeedbackGiven/Approved, same spacing/case rules; the existing critique functions are unchanged). The `/*BCP_ID_FILTER*/` marker was added to `st_history_comments.sql` and `st_critiquer_assignments.sql`, and `with_bcp_id_filter()` in `backend/app/db/queries.py` writes only `?` placeholders and returns the ids as bound parameters (1–100 positive ints). New `backend/app/history/extract.py` (Stress Test number, Python markers and agreement with the SQL hint, critiquer matching, a counts-only `ExtractSummary`, atomic CSV writing) and `backend/scripts/extract_st_history.py`. `backend/scripts/scan_for_credentials.py` now skips git-ignored files via `git check-ignore --stdin -z` (30 s timeout; tracked files are always scanned; if git is unavailable it scans everything), with the user's approval; this changes the REQ-016 / STORY-011 guardrail script. Renamed `BCP_ID_FILTER_TOKEN` to `BCP_ID_FILTER_MARKER` (it tripped the scanner's secret-name rule). Directive updated (status, Step 5 command, verification results).
  - Verification: `pytest` 157/157 (new: `history/test_extract.py` 8, filter and marker tests, `scripts/test_scan_for_credentials.py` 3 using a temporary git repo). Real run: `extract_st_history.py --bcp-ids 2148,2075,2083,2427,2275,2333,2311,2281,2264,2058,2071,1803,1779,1778,1272` → exit 0, VERIFICATION OK: 1,450 comments, 0 duplicate CommentIds, 15/15 projects, 87 threads, 0 marker disagreements, 82 critiquer assignments; ~99 s. `git check-ignore` confirms `data/extracts/` is ignored. `scan_for_credentials.py` → clean (85 files, 7 git-ignored skipped).
  - Notes: The extract contains personal data (student and reviewer names, emails, submission text) and lives only in the git-ignored `data/extracts/2026-09-25/`. Nothing was printed except counts. The runtime is close to the 120 s query timeout (the dedup window runs over the whole comments table); this is documented in the directive, and the fix if needed is raising `DB_QUERY_TIMEOUT_S` or pre-filtering the window by MessageId.

## Pre-commit review of the SQL Server / history-extraction work (user-requested)

- [x] Review all uncommitted code for defects and fit with the other stories; fix what was found
  - Date: 2026-09-25
  - Session: CC-20260924-e18o
  - What changed: (1) **Every query now logs** `db_query_completed` / `db_query_failed` (label, `execute_ms`, `duration_ms`, rows, error class; never the SQL text or parameters) in `backend/app/db/queries.py`; `fetch_metrics` reuses `fetch_table`. (2) **`StepHTML` moved out of the comment rows** into the new `st_history_steps.sql` / `steps.csv` (one row per step, joined on `ProjectDetailID`). Repeated per comment, it was 9 MB of a 16 MB extract; the extract is now 7.8 MB + 0.56 MB. Verification also fails if a comment's step is missing. (3) **NULL `CommentId` rows are excluded** from the de-duplication window (none exist today; they would otherwise collapse into one partition). (4) `check_db_connection.py` **no longer fails on text-typed `MessageId`/`MessageBoardID`** (reported as a note, because every query uses `TRY_CAST`); `CommentId` must still be an integer. (5) `.env.example` explains `DB_TRUST_SERVER_CERTIFICATE`. (6) README gains the SQL Server commands. `docs/ACCEPTANCE_CHECKLIST.md` REQ-013 now records partial progress (retrieval built; storage, a least-privilege login and a plan story still open) and stays unticked. The directive was updated with the timing facts and the new verification rule.
  - Verification: `pytest` 164/164. Real server: `check_db_connection.py` exit 0 (`RESULT: OK`, 2 notes). `extract_st_history.py` exit 0, VERIFICATION OK, 1,450 comments, 0 duplicates, 89 steps, 0 comments without a step. Re-running the extraction produced byte-identical CSVs (`shasum -c`) and left no temp files. `scan_for_credentials.py` clean (86 files, 8 git-ignored skipped). No file under `data/` or `.env` is untracked-but-committable.
  - Notes: **Correction:** my earlier note blamed the 99 s extraction on the whole-table de-duplication window, and that was wrong. Measured, the window takes 0.4 s and each query executes in about 1 s; the rest is fetching rows at about 150–200 KB/s from this server, which varies by run (10–60 s). The query timeout bounds execution, not fetch. Fit with the other stories: STORY-001 is unaffected; `BasecampComment.message_id` stays an integer (a future SQL Server comment source must `TRY_CAST` `MessageId`, as the history queries do), and duplicate snapshots are already absorbed by its `comment_id` idempotency. STORY-002 (Basecamp API/OAuth) is a separate retrieval path with no overlap. STORY-003 (the ST0 rule module) is next and takes the ST0 slice of this extract as input. REQ-013 still has no owning story in `plan.json`: raise it in the portal rather than editing the plan by hand.
